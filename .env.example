# PostgreSQL local (ver AGENTS.md -> Cómo correr)
DATABASE_URL="postgresql://usuario:password@localhost:5432/obrix?schema=public"

# Auth.js v5 - secreto para firmar sesiones JWT (generar con `openssl rand -base64 32`)
AUTH_SECRET="reemplazar-con-un-secreto-generado"
